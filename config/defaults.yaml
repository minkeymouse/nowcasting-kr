# Main Hydra configuration
# Composes configs from model/, data/, and dfm/ subdirectories

defaults:
  - model: default
  - data: default
  - dfm: default

# Experiment metadata
experiment_name: dfm_training
output_dir: outputs

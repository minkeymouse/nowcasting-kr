defaults:
  - _self_
  - model: dfm/investment  # Use nested path for data-specific DFM configs
  # Override via CLI: model=dfm/investment, model=dfm/production, model=ddfm, etc.
  - experiment: null  # Optional: can be overridden with experiment=short_term or experiment=long_term

# Training and forecasting flags
data: investment
train: true
forecast: true

# langchain

Use the docs-langchain mcp to get information regarding the agent system. This MCP provides documentation for langchain, and langgraph implementations regarding the docker compose setup, tools, nodes, edges, runtime, middleware, create_agent, subgraphs, multiagent, persistence and many others.